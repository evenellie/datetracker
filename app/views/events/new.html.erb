<%= form_for(@event) do |f| %>
  <p>
    <%= f.label :event, "Date" %><br>
    <%= f.date_select(:event, :order => [:month, :day, :year]) %>
  </p>

  <p>
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </p>

  <%= f.submit "Save the Date" %>
<% end %>
